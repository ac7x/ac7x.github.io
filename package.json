{
    "name": "deploy-to-vercel",
    "version": "1.0.0",
    "scripts": {
      "up": "git add --all && git commit -m \"Initial commit\" && git push -u origin main",

      "deploy": "echo 'This project is deployed via GitHub Actions and Vercel.'"
    },
    "dependencies": {
      "pnpm": "^10.6.5"
    },
    "devDependencies": {
      "actions/setup-node": "^4.0.0",
      "actions/checkout": "^4.0.0",
      "amondnet/vercel-action": "^25.0.0"
    }
  }
  