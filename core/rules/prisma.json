{
  "$schema": "./../shared/lint-config.json",
  "rules": [
    {
      "id": "require-select",
      "pattern": "prisma\\.\\w+\\.\\w+\\(\\s*\\{[^}]*?(?<!select|include)\\s*\\}\\)",
      "errorLevel": "block",
      "message": "Prisma query must explicitly specify select/include",
      "documentation": "https://ac7x.github.io/prisma-security",
      "examples": {
        "invalid": "await prisma.user.findMany({ where: { active: true } })",
        "fixed": "await prisma.user.findMany({ where: { active: true }, select: { id: true, name: true } })"
      }
    }
  ]
}