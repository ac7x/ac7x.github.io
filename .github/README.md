Next.js 專案開發規範

=== 架構設計 ===

01. **Next.js 基礎架構：** 框架與路由系統
- 採用 Next.js 14 框架並使用 App Router 路由系統
- 透過 Server Actions 直接執行後端邏輯，無需額外設置 API Routes
- 動態路由避免過度使用 generateStaticParams 導致構建膨脹
- 區分 loading.tsx 與 error.tsx 的粒度控制

02. **架構分層原則：** 分層架構與關注點分離
- 嚴格區分前端渲染與後端邏輯，Server Actions 集中處理業務邏輯
- 模組化設計，降低耦合度，提升可維護性
- 大型應用中進一步拆分業務邏輯層級
- 服務層抽象化避免直接依賴 Prisma
- 核心業務邏輯應可獨立測試 (如抽離為純函數)

03. **客戶端最小化策略：** 最小化客戶端原則
- 前端僅負責 UI 渲染與事件處理
- 禁止在前端元件中直接操作資料庫
- 所有數據處理與業務邏輯由 Server Actions 處理
- 表單操作需使用 useTransition 防重複提交
- 靜態頁面避免混用動態 Server Actions

=== 資料庫規範 ===

04. **Prisma 設定與使用：** Prisma 整合
- 使用 @prisma/client 與自動型別生成
- 所有資料庫操作通過 Prisma 執行
- 資料庫變更需同步版本控制與遷移
- 禁止手動修改 migration.sql 檔案
- 重大結構變更需附加回滾腳本

05. **資料庫連線管理：** 連線管理
- 根據併發量配置連線池參數 (如 connection_limit)
- 單例模式管理 PrismaClient 實例
- 生產環境啟用 shadowDatabaseUrl 預防遷移失敗
- 無伺服器環境需配置 prisma.$connect() 預熱機制

06. **資料庫查詢優化：** 效能優化
- 大量數據使用分頁機制 (推薦 cursor-based 分頁)
- 避免過度嵌套的 include/select
- 禁用 SELECT * 全欄位抓取 (明確指定 select)
- 批次化高頻寫入操作
- 定期執行 prisma.$queryRaw 原生 SQL 效能分析

07. **事務與資料一致性：** 事務與一致性
- 多操作場景必須使用 Prisma 事務
- 針對 N+1 問題實施自動檢測機制
- 高併發情境實施鎖機制/樂觀鎖

=== 安全性規範 ===

08. **使用者輸入驗證：** 輸入驗證
- 嚴格過濾用戶輸入內容
- 即使使用 Prisma 仍需手動驗證參數
- 嚴格驗證 FormData 結構 (建議搭配 Zod 解析)

09. **權限與授權控制：** 授權控制
- Server Actions 內置 RBAC 權限檢查
- 敏感操作需雙重驗證上下文
- 啟用 CSRF Token 雙重驗證 (Next.js 14 內建機制)

10. **資料序列化安全：** 序列化防護
- 轉換 BigInt/Date 等非 JSON 格式
- 禁止直接傳遞資料庫實體物件
- 自訂 toJSON 方法處理循環參照
- 敏感欄位需在 Prisma 層級排除 (如 select: { password: false })

=== 程式碼品質 ===

11. **TypeScript 型別規範：** 型別安全
- 充分利用 Prisma 自動型別推導
- 複雜查詢需明確定義返回型別
- 禁止使用 any 型別
- 使用 satisfies 運算子強化型別推論
- 建立 ResponseDTO 統一包裹 Server Actions 返回格式

12. **錯誤處理機制：** 錯誤處理
- Server Actions 必須包含 try/catch
- 實現錯誤分級處理與回退機制
- 關鍵操作需記錄完整錯誤日誌
- 錯誤分類範例：
  class AppError extends Error {
    constructor(
      public readonly type: 'FORBIDDEN' | 'NOT_FOUND' | 'VALIDATION',
      message: string
    ) {
      super(message)
    }
  }
- 前端需對應不同錯誤類型顯示 UI 狀態

13. **應用程式監控：** 效能監控
- 實施請求響應時間追蹤
- 整合 Sentry/Datadog 等監控工具
- 日誌安全規範：禁止記錄完整請求體，實施敏感詞過濾

=== 進階實踐 ===

14. **快取機制與策略：** 快取策略
- 針對高頻查詢實施資料快取
- 結合 Next.js 內建快取機制
- 資料異動時主動清除相關路由快取 (revalidatePath)
- 高頻介接第三方 API 需設置短週期 stale-while-revalidate

15. **併發處理策略：** 併發控制
- 限制高消耗操作的並發數量
- 實施請求佇列機制
- 串流渲染策略：延遲載入區塊使用 Suspense 與骨架屏

16. **第三方依賴管理：** 依賴管理
- 定期更新第三方套件
- 重大版本升級需完整測試
- Bundle 管控：定期執行 @next/bundle-analyzer 檢測

17. **程式碼文件與測試：** 文件規範
- 複雜查詢需添加 SQL 註解
- 自定義型別需補充 JSDoc 說明
- 測試策略：
  - 使用 @testing-library/react 測試互動邏輯
  - 對 Server Actions 實施端到端測試 (搭配 Playwright)

=== 團隊協作規範 ===

18. **Git 提交規範：** Commit 管控
- 資料庫遷移檔案需獨立提交 (禁止與業務邏輯混合)
- 包含 Server Actions 修改需附加影響範圍說明

19. **Code Review 準則：** Code Review 重點
- 特別檢查 Server Actions 的錯誤回滾完整性
- 驗證 Prisma 查詢是否觸發全表掃描 (EXPLAIN 分析)

20. **開發環境隔離：** 環境隔離策略
- 建立統一 Docker 開發環境避免本地資料庫版本差異
- 預生產環境需啟用與正式環境相同規格的連線池

1. **資料庫連線池配置：** 即使使用單例模式來避免每次請求都重新初始化 PrismaClient，但在高併發情況下，資料庫的連線池配置仍然很重要。應該根據預期的併發量來調整 Prisma 的 `connection_limit`，以避免因連線數過多導致資料庫性能瓶頸或錯誤。

2. **大量數據導致的性能瓶頸：** 在查詢大數據集時，儘管使用了分頁和緩存策略，還是可能會遇到性能瓶頸。可以考慮使用 `cursor` 分頁（基於游標的分頁）來減少資料庫查詢壓力，並且避免多次查詢所帶來的延遲。

3. **避免高頻繁的資料庫寫入：** 在多次資料庫寫入的情境下，應小心頻繁的 I/O 操作帶來的性能問題。應考慮將寫入操作批次化，使用 Prisma 的事務機制來保證操作的一致性並減少不必要的寫入頻次。

4. **異常處理和回退機制：** 儘管有異常處理機制，但在高負載的情況下，未處理的例外仍然可能發生。應該考慮在錯誤發生時實現回退機制（如重試邏輯或回滾機制），特別是對於某些不可預測的錯誤（如資料庫過載或網路中斷）應能自動處理。

5. **資料一致性與並發操作：** 在多人同時操作相同資料的情境下，可能會出現資料競爭或不一致的問題。應該使用資料庫的鎖定機制或樂觀鎖來確保資料的一致性，避免同時更新同一條資料引起的錯誤。

6. **API 安全性：** 雖然所有的後端邏輯都已經放置在 Server Actions 中，但仍需對用戶的身份進行徹底的驗證，並確保敏感操作有適當的授權檢查。可以考慮使用基於角色的訪問控制（RBAC）來進一步強化授權。

7. **第三方依賴的管理與更新：** 隨著專案的發展，第三方庫和工具（如 Prisma、Next.js）也會不斷更新，這可能導致不兼容的問題。應定期檢查依賴版本並執行測試，確保新版本的兼容性。

8. **使用 TypeScript 的類型安全：** 雖然 Prisma 提供了自動型別生成，但對於複雜查詢或動態查詢結構，還是可能存在型別錯誤或類型不一致的問題。要確保所有動態查詢和條件的型別都經過嚴格檢查，並使用 TypeScript 的型別推導來防止錯誤。

9. **大型應用中的分層架構：** 隨著專案規模的增長，應該考慮進一步拆分 Server Actions，將不同的業務邏輯層級劃分清楚，避免邏輯過於集中在單一層級，從而提高代碼的可維護性。

10. **應用錯誤監控與日志管理：** 為了避免未來出現未知的錯誤或問題，應實現錯誤監控和日志管理，並將錯誤和性能指標傳送至監控系統（如 Sentry、Datadog 等），以便及時發現和處理問題。

11. **框架與路由系統：** 本專案使用最新版本的 Next.js 框架，並採用其路由系統來處理頁面導向。後端邏輯透過伺服器端函式（Server Actions）直接執行，無需額外設置 API 路由。

12. **資料庫操作與型別定義：** 使用 Prisma 作為資料庫操作工具，並結合自動生成的型別定義來保證資料一致性與開發效率，避免手動處理型別定義。

13. **單例模式：** 為避免每次請求都重新初始化資料庫連線，使用單例模式來保持資料庫連線的有效性，防止連線過多引發錯誤。

14. **最小化客戶端邏輯：** 嚴格遵守最小化客戶端的原則，將業務邏輯放在伺服器端處理，前端只負責 UI 渲染與用戶互動，從而提升效能與安全性。

15. **資料處理與轉換：** 在伺服器端處理來自資料庫的資料時，對於無法直接序列化的資料類型（如大數字或日期）進行適當轉換，確保資料能夠安全傳遞至前端。

16. **錯誤處理機制：** 在伺服器端邏輯中，必須具備充分的錯誤處理機制，避免未處理的錯誤導致應用崩潰或資料不一致，並確保異常情況下的恢復機制（如重試或回滾）。

17. **資料庫操作的原子性與一致性：** 對於涉及多個資料庫操作的場景，應使用事務來保證操作的完整性，避免操作部分成功、部分失敗的情況。

18. **查詢與效能優化：** 在處理大量資料時，應避免過多的同步查詢，並使用分頁機制或緩存來提高效能。查詢結構應保持簡潔，避免不必要的資料載入。

19. **API 安全性與授權檢查：** 雖然後端邏輯放在伺服器端處理，但仍需對用戶的身份進行徹底驗證，並確保敏感操作有適當的授權控制，避免未授權訪問。

20. **型別安全與錯誤檢查：** 雖然資料庫操作會自動生成型別，但對於動態查詢或複雜查詢，應確保型別的嚴格檢查，避免由於型別不匹配導致的錯誤。

21. **資料庫結構與版本控制：** 資料庫的變更應該與應用程式代碼一同進行版本控制，並在修改資料庫結構時進行遷移，確保資料庫的變更不會影響到現有的運行環境。

22. **高效的資料庫寫入與處理：** 在高併發或頻繁寫入的情況下，應批次處理寫入操作，減少不必要的 I/O 操作並提升效能。

23. **並發控制與資料一致性：** 在高併發情境下，應該使用鎖機制或樂觀鎖來避免資料競爭，保證資料的一致性和正確性。

24. **錯誤監控與日志管理：** 實現錯誤監控與日志管理系統，對應用運行中的錯誤與性能問題進行及時發現和處理，保證系統的穩定運行。

25. **型別與接口設計：** 在動態查詢與查詢結果的處理過程中，應確保查詢型別與結果型別的一致性，避免錯誤的資料傳遞，並能夠在編譯階段捕捉到潛在錯誤。
