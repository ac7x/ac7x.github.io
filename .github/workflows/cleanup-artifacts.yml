name: 清理過期構建產物

on:
  schedule:
    - cron: '0 0 * * 0'  # 每週日午夜運行
  workflow_dispatch:  # 允許手動觸發

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: 清理過期構建產物
        uses: c-hive/gha-remove-artifacts@v1
        with:
          age: '7 days'
          skip-recent: 5
